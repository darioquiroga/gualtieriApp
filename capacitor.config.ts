import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gualtieriehijos',
  appName: 'gualtieriehijos',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
